import React from 'react'
import ReactDOM from 'react-dom/client'
import Waiting from './GSAP/Waiting'
import './main.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Waiting></Waiting>
  </React.StrictMode>,
)
